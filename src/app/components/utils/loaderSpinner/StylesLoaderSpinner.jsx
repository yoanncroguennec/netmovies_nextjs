import { Box, styled } from "@mui/material";

export const Root_LoaderSpinner = styled(Box)(({ theme }) => ({
  alignItems: "center",
  display: "flex",
  height: "100vh",
  justifyContent: "center",
  width: "100vw",
}));
